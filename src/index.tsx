export * from './dataProvider'
export * from './hooks/useColumns'
export * from './components/AdminGuesser'
export * from './components/RecordGuesser'

// TODO: suggest code for custom Record components (like api-platform admin does)
// TODO: guess foreign key references (using reference fields and inputs)
// TODO: better numeric support (floats should not be allowed for int columns)
// TODO: guess basic validation rules (e.g. emails, urls, colors, slugs, etc)
